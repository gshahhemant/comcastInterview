package com.helloworld.validator;

import org.apache.commons.lang3.StringUtils;
import org.springframework.stereotype.Service;

/*

@Service
public class Validator {

	public boolean appValidation(AppInfo appInfo) {

		if (appInfo == null || StringUtils.isBlank(appInfo.getName()) ||StringUtils.isBlank(appInfo.getPublisherName()) || StringUtils.isBlank(appInfo.getShortDescription())
				|| StringUtils.isBlank(appInfo.getLongDescription()) || StringUtils.isBlank(appInfo.getPublisherCompanyName()) || appInfo.getMajorVersion() < 0
				|| appInfo.getMinorVersion() < 0 || appInfo.getPatchVersion() < 0) {

			return false;

		} 			
		return true;
	}

}
*/